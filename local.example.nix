{ ... }: {
  # Kilin (my laptop): imports = [ ./desktops/kilin.nix ];
  # Arkilin (my desktop): imports = [ ./desktops/arkilin.nix ];
  # Sydkilin (my server): imports = [ ./servers/sydkilin.nix ];

  # You choose (see NixOS doc)
  system.stateVersion = "19.03";
}
